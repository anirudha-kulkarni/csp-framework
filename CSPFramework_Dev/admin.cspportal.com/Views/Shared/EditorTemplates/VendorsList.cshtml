@using CSPLibrary
@using admin.cspportal.com.Helpers
@{
    String selectedVal = "";
    if (@Model != null)
    {
        selectedVal = (String)@Model.ToString();
    }

    var items = DropDonwHelper.GetItems("GetVendorsList", selectedVal);

}

@Html.DropDownList("", items, new { @id = "vendorslist_" })

