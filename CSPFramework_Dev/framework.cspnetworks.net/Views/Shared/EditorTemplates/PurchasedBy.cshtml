@using CSPLibrary
@using framework.cspnetworks.net.Helpers
@{
    String selectedVal = "";
    if (@Model != null)
    {
        selectedVal = (String)@Model.ToString();
    }

    var items = DropDonwHelper.GetItems("GetPurchaserList", selectedVal);

}

@Html.DropDownList("", items, new { @class = "form-control", @id = "purchaserlist_" })
