@using CSPLibrary
@using framework.cspnetworks.net.Helpers
@{
    String selectedVal = "";
    if (@Model != null)
    {
        selectedVal = (String)@Model.ToString();
    }

    var items = DropDonwHelper.GetItems("GetSoftware_LicenseList", selectedVal);
}

@Html.DropDownList("", items, new { @class = "form-control", @id = "software_licenselist_" })
