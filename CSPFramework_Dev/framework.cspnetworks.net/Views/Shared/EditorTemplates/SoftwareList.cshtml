@using CSPLibrary
@using framework.cspnetworks.net.Helpers
@{
    String selectedVal = "";
    if (@Model != null)
    {
        selectedVal = (String)@Model.ToString();
    }

    var items = DropDonwHelper.GetItems("GetSoftwareItemList", selectedVal);
}

@Html.DropDownList("", items, new { @id = "softwarelist_" })
